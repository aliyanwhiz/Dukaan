const initailState = {
    loading: false,
    cartItems: []
}

export const rootReducer = (state=initailState,action) => {
    switch (action.type) {
        
        default:
            return state;
    }
}