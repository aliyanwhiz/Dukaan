const initailState = {
    loading: false,
    cartItems: []
}

export const rootReducer = (state=initailState,action:any) => {
    switch (action.type) {
        
        default:
            return state;
    }
}